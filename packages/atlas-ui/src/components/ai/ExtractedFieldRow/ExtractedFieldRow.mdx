import { Meta, Canvas, Controls, ArgTypes } from '@storybook/addon-docs/blocks';
import * as ExtractedFieldRowStories from './ExtractedFieldRow.stories';
import { DocsLayout, DocsTitle, DocsIntro, DocsSection } from '../../../docs/components/DocsLayout';

<Meta of={ExtractedFieldRowStories} />


<DocsLayout>

<DocsTitle>Extracted Field Row</DocsTitle>

<DocsIntro>
Source: Atlas Product — Document Review's Extracted Fields card
(`field-row`): a label + value stack on the left, a Confidence Badge on
the right.
</DocsIntro>

<DocsSection title="Overview">

The single-row primitive Document Review's Extracted Fields card
repeats for every extracted value. Composes Confidence Badge rather
than reimplementing the percentage-and-chip treatment.

</DocsSection>

<DocsSection title="Live Preview">

<Canvas of={ExtractedFieldRowStories.Default} />

</DocsSection>

<DocsSection title="Usage">

Stack multiple rows in a card, separated by a divider, to build the
full Extracted Fields list.

</DocsSection>

<DocsSection title="Variants">

<Canvas of={ExtractedFieldRowStories.FieldList} />

</DocsSection>

<DocsSection title="Sizes">

One size, sized for a ~300px-wide fields sidebar.

</DocsSection>

<DocsSection title="States">

Static by default. Pass `editable` to add a Pencil affordance that
turns the value into an inline text field — Enter or blur commits via
`onSave`, Escape reverts. The row doesn't decide what a correction
means (re-running confidence, persisting it, etc.) — that's the
consuming screen's job via `onSave`.

On a successful commit, the row briefly flashes a success-tinted
background (respecting `prefers-reduced-motion`, like every other Atlas
transition) and announces the update to assistive tech via a visually
hidden status message — a save that only changes pixels a sighted user
happens to be looking at isn't confirmed for everyone.

<Canvas of={ExtractedFieldRowStories.Editable} />

</DocsSection>

<DocsSection title="Accessibility">

- Label and value are plain text, readable by assistive tech in
  document order.
- Confidence Badge carries its own `aria-label` with the full
  confidence sentence.
- The edit button has an explicit `aria-label` ("Edit {label}") since
  a bare Pencil icon has no accessible name on its own; the inline
  input is associated with the label via a real `<label htmlFor>`.
- The edit affordance is hover-revealed but never `display:none` — it
  reaches full opacity on keyboard focus, so Tab still finds it.

</DocsSection>

<DocsSection title="React Props">

<Controls of={ExtractedFieldRowStories.Default} />

<ArgTypes of={ExtractedFieldRowStories} />

</DocsSection>

<DocsSection title="Examples">

```tsx
import { ExtractedFieldRow } from '@/components/ai/ExtractedFieldRow';

<ExtractedFieldRow label="Vendor Name" value="Acme Corporation" confidence={98} />
```

</DocsSection>

<DocsSection title="Best Practices">

<div className="atlas-doc-dodont">
  <div className="atlas-doc-dodont__card atlas-doc-dodont__card--do">
    <span className="atlas-doc-dodont__label">Do</span>
    <p>Sort or highlight low-confidence rows so a reviewer can triage them first.</p>
  </div>
  <div className="atlas-doc-dodont__card atlas-doc-dodont__card--dont">
    <span className="atlas-doc-dodont__label">Don't</span>
    <p>Truncate the value without a way to see the full text — long values should still be inspectable.</p>
  </div>
</div>

</DocsSection>

<DocsSection title="Related Components">

<div className="atlas-doc-related">
  <a className="atlas-doc-related__link" href="?path=/docs/ai-confidence-badge--docs">Confidence Badge</a>
  <a className="atlas-doc-related__link" href="?path=/docs/data-display-detail-list--docs">Detail List</a>
</div>

</DocsSection>

</DocsLayout>
