import { Meta, Canvas, Story, Controls, ArgTypes } from '@storybook/addon-docs/blocks';
import * as NotificationPanelStories from './NotificationPanel.stories';
import { DocsLayout, DocsTitle, DocsIntro, DocsSection } from '../../../docs/components/DocsLayout';

<Meta of={NotificationPanelStories} />


<DocsLayout>

<DocsTitle>Notification Panel</DocsTitle>

<DocsIntro>
The bell-icon panel present in Atlas Product's topbar on every screen —
a 36px chip with a small corner dot indicating unread notifications.
Composes Popover for the floating panel rather than a custom
implementation.
</DocsIntro>

<DocsSection title="Overview">

A bell trigger (with an optional unread dot) that opens a scrollable
list of notifications, each with a title, optional description, and a
relative time.

</DocsSection>

<DocsSection title="Live Preview">

<Canvas of={NotificationPanelStories.Default} />

</DocsSection>

<DocsSection title="Usage">

Pass `items` and set `hasUnread` when any of them are new — this drives
both the trigger's corner dot and its accessible name. Unread items
(`read: false`) get a tinted background so they're scannable at a
glance without relying on the corner dot alone.

</DocsSection>

<DocsSection title="Variants">

<Canvas of={NotificationPanelStories.Unread} />

</DocsSection>

<DocsSection title="Sizes">

One size (320px panel width, capped at 320px scrollable height for the
list).

</DocsSection>

<DocsSection title="States">

<Canvas of={NotificationPanelStories.Empty} />

</DocsSection>

<DocsSection title="Accessibility">

- The trigger's accessible name changes between "Notifications" and
  "Notifications (unread)" depending on `hasUnread` — the unread state
  is never conveyed by the corner dot alone.
- Each notification is a real `<button>`, so it's reachable and
  activatable by keyboard, not a `<div>` with a click handler.

</DocsSection>

<DocsSection title="React Props">

<Controls of={NotificationPanelStories.Default} />

<ArgTypes of={NotificationPanelStories} />

</DocsSection>

<DocsSection title="Examples">

```tsx
import { NotificationPanel } from '@/components/overlays/NotificationPanel';

<NotificationPanel
  hasUnread
  items={[
    { id: '1', title: 'Workflow published', description: 'Invoice Approval is now live.', time: '2m ago', read: false },
    { id: '2', title: 'Weekly digest ready', time: 'Yesterday', read: true },
  ]}
  onItemClick={(id) => markRead(id)}
/>
```

</DocsSection>

<DocsSection title="Best Practices">

<div className="atlas-doc-dodont">
  <div className="atlas-doc-dodont__card atlas-doc-dodont__card--do">
    <span className="atlas-doc-dodont__label">Do</span>
    <p>Mark items read as the user views them, so the unread dot and tinted rows stay accurate over time.</p>
  </div>
  <div className="atlas-doc-dodont__card atlas-doc-dodont__card--dont">
    <span className="atlas-doc-dodont__label">Don't</span>
    <p>Rely on the corner dot alone to signal unread state — it's paired with the trigger's accessible name and each row's tinted background for exactly this reason.</p>
  </div>
</div>

</DocsSection>

<DocsSection title="Related Components">

<div className="atlas-doc-related">
  <a className="atlas-doc-related__link" href="?path=/docs/overlays-popover--docs">Popover</a>
  <a className="atlas-doc-related__link" href="?path=/docs/overlays-user-menu--docs">User Menu</a>
</div>

</DocsSection>

</DocsLayout>
