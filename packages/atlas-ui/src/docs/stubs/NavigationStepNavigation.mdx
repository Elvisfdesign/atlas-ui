import { Meta } from '@storybook/addon-docs/blocks';
import { ComingSoon } from '../ComingSoon';
import { DocsLayout, DocsTitle } from '../components/DocsLayout';

<Meta title="Navigation/Step Navigation" />

<DocsLayout>

<DocsTitle>Step Navigation</DocsTitle>

<ComingSoon status="Not yet confirmed as a distinct component in the Atlas Figma files. This session's Figma access was limited to the Atlas Product file's Review Queue screen and the Atlas UI System's Cover page — neither shows a multi-step flow. Before building this, a Figma-side inventory pass is needed (e.g. checking Workflow Builder or an onboarding flow for a stepper pattern) so this doesn't become a fabricated component with no product basis." />

</DocsLayout>
