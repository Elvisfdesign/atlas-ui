import { Meta } from '@storybook/addon-docs/blocks';
import { ComingSoon } from '../ComingSoon';

<Meta title="Templates" />

# Templates

<ComingSoon status="Full page templates land here once Data Table, Forms, and Navigation are implemented — building a template on primitives that don’t exist yet would be exactly the kind of one-off, unreproducible example this library tries to avoid." />
