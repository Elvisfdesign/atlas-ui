import { Meta } from '@storybook/addon-docs/blocks';
import { ComingSoon } from '../ComingSoon';
import { DocsLayout, DocsTitle } from '../components/DocsLayout';

<Meta title="Patterns" />

<DocsLayout>

<DocsTitle>Patterns</DocsTitle>

<ComingSoon status="Composed, multi-component examples land here once enough primitives exist to build them honestly. The Atlas UI System’s own Patterns page documents these at the design level today." />

</DocsLayout>
