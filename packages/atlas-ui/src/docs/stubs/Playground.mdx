import { Meta } from '@storybook/addon-docs/blocks';
import { ComingSoon } from '../ComingSoon';
import { DocsLayout, DocsTitle } from '../components/DocsLayout';

<Meta title="Playground" />

<DocsLayout>

<DocsTitle>Playground</DocsTitle>

<ComingSoon status="A space for stress-testing components together — long content, narrow containers, loading and empty states side by side. More useful once there’s more than six components to test interactions between." />

</DocsLayout>
